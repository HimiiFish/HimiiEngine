# Agent 约束

面向使用 AI 编程的引擎贡献者。

## 规范源

**唯一共享约束**在仓库根目录的 `AGENTS.md`（命名、改动边界、成套交付、Inspector / 产品 UX）。

文档站点不托管该文件；请在仓库根目录直接打开 `AGENTS.md`。

## 本机 IDE 适配

`.cursor/`、`.claude/`、`.codex/`、`.agents/` 等为各工具本机配置，**不入库**。  
本仓库**不再使用** Trellis（`.trellis/` 已弃用且已加入 `.gitignore`）。
